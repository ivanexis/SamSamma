# 部署與上傳檢查清單（GitHub → Cloudflare）

適用專案：**`ivanexis/ivanzhao-portfolio`**（分支：`main`）  
本地路徑：`C:\Blog\blog_uiux`  
線上網域：**https://ivanzhao.dpdns.org/**

參考連結：
- 倉庫：[https://github.com/ivanexis/ivanzhao-portfolio](https://github.com/ivanexis/ivanzhao-portfolio)
- 舊倉庫（僅備份遠端名 `blog_uiux_backup`）：[`ivanexis/blog_uiux`](https://github.com/ivanexis/blog_uiux)

---

## 0. 推送紀錄（每次 push 後更新這段）

| 項目 | 值 |
|------|-----|
| 遠端 `origin` | `https://github.com/ivanexis/ivanzhao-portfolio.git` |
| 分支 | `main` |
| 最近提交 | `ee7a4f2` — feat: cases copy, WebDesign IP disclosure, deploy docs |
| 推送時間 | 2026-05-31 |
| Cloudflare | Workers Builds：`ivanzhao-portfolio`；Deploy：`npx wrangler deploy` |
| 自訂網域 | `ivanzhao.dpdns.org` |

**日常推送（標準流程）：**

```powershell
cd C:\Blog\blog_uiux
git status
git add -A
git status
git commit -m "feat: 簡述本次變更"
git push origin main
git log -1 --oneline
```

**確認遠端與追蹤：**

```powershell
git remote -v
git branch -vv
```

預期：`origin` → `ivanzhao-portfolio`；`main` 顯示 `[origin/main]`。

---

## 1. 什麼上傳 GitHub？什麼不上傳？

| 內容 | 是否進 GitHub | 說明 |
|------|----------------|------|
| `index.html`、`cases.html`、`pages/`、`js/`、`css/`、`scss/` | **是** | 網站程式 |
| `images/` | **是** | 本站用圖 |
| **`Web_design/`（含 `optimized/`）** | **是（本私密倉庫）** | 已追蹤約 324 檔；`.gitignore` 註解為私密作品集用途 |
| `pages/專案狀態/` | **否** | 面試筆記 |
| `.wrangler/`、`.dev.vars` | **否** | Wrangler 本機快取 |
| `wrangler.jsonc`、`.assetsignore` | **是** | Workers 部署設定；`.assetsignore` 排除 `.git` 等 |

**Workers 部署必備（勿刪）：**
- `wrangler.jsonc` — 靜態 assets 目錄 `.`
- `.assetsignore` — 排除 `.git/`、`node_modules/` 等（避免 25MB 單檔錯誤）

---

## 2. 推送前快速檢查

```powershell
cd C:\Blog\blog_uiux

# 不應提交私人筆記
git status
# 確認沒有 pages/專案狀態/

# 可選：確認 Web_design 是否在版控（本 repo 有）
git ls-files Web_design | Select-Object -First 3

git diff --stat
git log --oneline -3
```

---

## 3. 首次／換電腦設定

```powershell
cd C:\Blog\blog_uiux
git clone https://github.com/ivanexis/ivanzhao-portfolio.git
# 若已有資料夾僅補 remote：
git remote add origin https://github.com/ivanexis/ivanzhao-portfolio.git
git branch -M main
git pull origin main
```

---

## 4. Cloudflare（目前用法）

| 項目 | 建議值 |
|------|--------|
| GitHub 來源 | `ivanexis/ivanzhao-portfolio` / `main` |
| Deploy command | `npx wrangler deploy`（Workers Static Assets） |
| 勿打包 | 依賴 `.assetsignore` 排除 clone 內的 `.git` |

未自動部署時：

```powershell
git commit --allow-empty -m "chore: trigger Cloudflare redeploy"
git push origin main
```

---

## 5. 部署驗證

- [ ] https://ivanzhao.dpdns.org/
- [ ] https://ivanzhao.dpdns.org/cases.html?lang=zh
- [ ] https://ivanzhao.dpdns.org/pages/WebDesign.html?id=web-design-1-1（商案聲明、內頁圖）
- [ ] https://ivanzhao.dpdns.org/pages/SamSamma.html?lang=zh

無痕開啟，避免快取誤判。

---

## 6. 常見問題

| 問題 | 處理 |
|------|------|
| Asset too large / `.git` pack | 確認 repo 根目錄有 `.assetsignore` 且已 push |
| 詳情頁沒圖 | 確認 `Web_design/**/optimized` 已 push |
| push 很慢 | 勿 `git add -f` 整包非 optimized 原稿 |
| 商標頁面 | 在 `work-detail-data.js` 移除或設 `hidden: true` |

---

## 7. 提交訊息範本

```text
feat: cases 文案、WebDesign 商案聲明 i18n
fix: 隱藏含商標內頁、wrangler assetsignore
docs: 更新部署清單與 push 紀錄
chore: trigger Cloudflare redeploy
```

---

## 8. 資料夾對照

```
blog_uiux/  （= GitHub 倉庫根目錄）
├── index.html, cases.html, pages/, js/  → push
├── images/                               → push
├── Web_design/**/optimized/              → push（本 repo）
├── wrangler.jsonc, .assetsignore         → push
└── pages/專案狀態/                       → 不 push（.gitignore）
```

---

## 9. 歷史推送摘要（手動維護）

| 日期 | Commit | 摘要 |
|------|--------|------|
| 2026-05-30 | `b23c009` | 作品集初版、雙語 SamSamma、i18n |
| 2026-05-30 | `00ba62c` | `.wranglerignore`（後改 `.assetsignore`） |
| 2026-05-30 | `d0fa0dc` | `.assetsignore` + `wrangler.jsonc` 修 deploy |
| 2026-05-31 | `ee7a4f2` | cases 文案、WebDesign 商案聲明 i18n、隱藏商標內頁、部署清單統一 |
