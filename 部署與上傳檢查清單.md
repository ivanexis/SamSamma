# 部署與上傳檢查清單（GitHub → Cloudflare Pages）

適用專案：`ivanexis/blog_uiux`（分支：`main`）  
本地路徑：`C:\Blog\blog_uiux`

參考連結：
- 倉庫：[`ivanexis/blog_uiux`](https://github.com/ivanexis/blog_uiux)
- 最新部署：[`09eb5fc5.blog-uiux.pages.dev`](https://09eb5fc5.blog-uiux.pages.dev)

---

## 0. 什麼上傳 GitHub？什麼不上傳？（必讀）

| 內容 | 是否進 GitHub | 說明 |
|------|----------------|------|
| `index.html`、`pages/`、`js/`、`css/`、`scss/` 等 | **是** | 網站程式與版面 |
| `images/`（本站用圖：logo、web1、海報、山莎蔓岸等） | **是** | 首頁、案例頁、地方創生**必須**有這包 |
| **`Web_design/` 整包** | **否** | 前公司／商案網站設計原稿與大圖；已寫入 `.gitignore` |
| `Web_design/**/optimized/` 壓圖（約 88MB） | 預設也不進 Git | 若要用 Git 部署詳情頁圖，見下方「方案 B」 |

**為何不上傳 `Web_design/`？**
- 非 `optimized` 原稿約 **5.7GB+**，GitHub 不適合、也易觸及商案保密。
- 程式只用到各作品底下的 **`optimized/`**（`work-detail-data.js` 的 `basePath`）。

**不上傳 `Web_design` 時，線上會怎樣？**
- 首頁「網站設計」卡片、靜態區（地方創生／海報）→ 正常（用 `images/`）。
- `WebDesign.html?id=web-design-x-x` 詳情頁、輪播 → **圖片 404**（除非另做方案 B）。

### 方案 A（目前預設）：Git 不含圖，主機另傳 `Web_design`

1. GitHub / Cloudflare 只部署程式 + `images/`。
2. 用 **FTP、R2、或 Cloudflare 手動上傳**，把整個 `Web_design`（至少各作品的 `optimized/`）放到網站根目錄，路徑與本地相同：  
   `https://你的網域/Web_design/1.Shopping websites/1/optimized/hero.jpg`

### 方案 B：只把 `optimized` 納入 Git（可選）

編輯 `.gitignore`，改為只追蹤壓圖資料夾（約 0.09GB），詳見檔內註解。  
推送後 Cloudflare 會一併部署詳情用圖，無需 FTP 補傳。

### `images/` 與「前公司商案圖」

- **商案長圖、多頁設計稿** → 在 `Web_design/`，已整包忽略。
- **`images/`** → 本站自用資源（非整包商案庫），**要進 GitHub**。勿刪除整個 `images/`。

---

## 1. 本地變更檢查

在 PowerShell 於 `C:\Blog\blog_uiux` 執行：

```powershell
cd C:\Blog\blog_uiux
git status
```

確認 `Web_design/` 不會出現在待提交列表（應顯示為 ignored）。  
若有其他未提交檔案再提交：

```powershell
git add -A
git status
git commit -m "docs: 部署清單與 gitignore，Web_design 不上傳 GitHub"
```

---

## 2. 首次建立倉庫並推送到 GitHub

若尚未初始化：

```powershell
cd C:\Blog\blog_uiux
git init
git branch -M main
git remote add origin https://github.com/ivanexis/blog_uiux.git
```

首次推送（遠端已有歷史時）：

```powershell
git pull origin main --allow-unrelated-histories --no-edit
git push -u origin main
```

日常推送：

```powershell
git add -A
git commit -m "feat: 說明變更內容"
git push origin main
```

確認遠端：

```powershell
git remote -v
```

---

## 3. 推送前快速檢查

```powershell
# 確認沒有誤把 Web_design 加進去
git ls-files Web_design
# 應無輸出

# 確認 optimized 路徑的 js 有進版控
git ls-files js/work-detail-data.js
```

```powershell
git log --oneline -3
```

---

## 4. 觸發 Cloudflare Pages 部署

- 推送至 `main` 通常會自動部署。
- 未觸發時：

```powershell
git commit --allow-empty -m "trigger: force Cloudflare Pages redeploy"
git push origin main
```

---

## 5. Cloudflare Pages 設定

專案：`blog-uiux`

| 項目 | 建議值 |
|------|--------|
| 來源 | GitHub `ivanexis/blog_uiux` / `main` |
| Build command | **留空**（靜態 HTML） |
| Output directory | `/` |
| Root directory | 若 monorepo 填 `blog_uiux/`；若倉庫根就是本站則 `/` |

**若採方案 A（Git 無 Web_design）**  
部署完成後，請用 FTP／R2 上傳 `Web_design` 至同一網域根目錄，或改採方案 B。

---

## 6. 部署驗證

- 主要網域：`blog-uiux.pages.dev`（或自訂網域）
- 必測：
  - [ ] `index.html` 首頁、作品列表
  - [ ] `pages/SamSamma.html`、`images/初步構想圖.jpg`
  - [ ] `pages/WebDesign.html?id=web-design-1-1`（需已上傳 `Web_design` 或方案 B）

無痕模式開啟，避免快取誤判。

---

## 7. 常見問題

| 問題 | 處理 |
|------|------|
| Git push 很大、很慢 | 確認 `.gitignore` 含 `Web_design/`，勿 `git add -f Web_design` |
| 詳情頁沒圖 | Git 未含 `optimized` → 用方案 A 補傳或改方案 B |
| `images/` 破圖 | 勿忽略整包 `images/`；檢查路徑大小寫 |
| 建置失敗 | 靜態站 Build command 留空 |
| 錨點／按鈕點不到 | 檢查覆蓋層 `pointer-events` |

---

## 8. 提交訊息範本

```text
feat: 能量招募錨點、CTA 點擊修復、產品圖片增亮
docs: 部署清單、gitignore 排除 Web_design 商案原稿
chore: trigger Cloudflare Pages redeploy
```

---

## 9. 本地資料夾對照（備忘）

```
blog_uiux/
├── index.html, pages/, js/     → 上傳 GitHub
├── images/                          → 上傳 GitHub（本站圖，非 Web_design 商案庫）
└── Web_design/                      → 不上傳 GitHub（.gitignore）
        └── */optimized/           → 線上詳情用圖；FTP 或方案 B
```

---

最近一次部署紀錄（請自行更新）：

- 分支：`main`
- 提交：—
- 狀態：—
- 頁面：[`blog-uiux.pages.dev`](https://blog-uiux.pages.dev)
